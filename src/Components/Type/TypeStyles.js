
const setTypeStyle = (primary, secondary, fontColor) => {
    const StyleComponent = {
        background: `linear-gradient(180deg, ${ primary } 50%, ${ secondary } 50%);`,
        color: fontColor
    }
}